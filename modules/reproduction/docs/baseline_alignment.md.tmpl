# Baseline 对齐核查：{{PAPER_TITLE}}

> 回答一个问题：**我的 setup 跟论文的 setup，真的是同一个吗？**
> 配套文件：`docs/reproduction_protocol.md`（参数对照与偏离清单）。
> 本文件的每一项都可能在**不报错**的情况下让数字不可比——所以必须逐项留下证据。

**created**: [TODO: YYYY-MM-DD]
**updated**: [TODO: YYYY-MM-DD]
**对应论文**: {{PAPER_TITLE}} ({{VENUE_YEAR}})
**对应复现方案**: `docs/reproduction_protocol.md`

---

## 使用方式

- 每一项都要填「**证据**」列——只写「一致」不算完成，要写**你怎么知道的**（命令输出、样本数、代码路径、hash）。
- 每次改动环境（换机器、升级依赖、改预处理）后**重跑本表**，并在 §7 追加一行。
- 任一栏出现 ❌ 时，**先停下**：带 ❌ 的 setup 跑出来的数字不能直接与论文比较。

---

## 1. 数据集版本与 split

| 检查项 | 论文 | 本次 | 一致？ | 证据 |
|---|---|---|---|---|
| 数据集名称 / 版本 | {{PAPER}} | {{MINE}} | ✅/❌ | {{EVIDENCE}} |
| 训练集样本数 | {{NUM}} | {{NUM}} | | `{{CMD}}` 输出 |
| 验证集样本数 | {{NUM}} | {{NUM}} | | |
| 测试集样本数 | {{NUM}} | {{NUM}} | | |
| 类别数 | {{NUM}} | {{NUM}} | | |
| split 文件来源 | {{PAPER}} | {{MINE}} | | {{PATH_OR_COMMIT}} |
| 每类样本分布是否一致 | [TODO: 是 / 否] | [TODO: 是 / 否] | | {{EVIDENCE}} |

**高频坑**：

- **同名数据集的不同版本**（v1 / v2 / 重新标注版）样本数可能差几十条 → 指标不可比。
- **split 不是随机的**：很多论文用固定 split 文件，你自己 random split 会得到不同结果，且**验证集难度不同**。
- **测试集与验证集混用**：部分论文报的是 val 上的数（因为 test 标签未公开），你若报 test 就不可比。
- `[TODO: 确认论文报的是 val 还是 test]`

**原始数据完整性**：

| 项 | 值 |
|---|---|
| 数据根目录 | `{{DATA_ROOT}}` |
| 文件数 / 总大小 | {{NUM}} / {{SIZE}} |
| 校验方式（md5 / 官方脚本） | {{METHOD}} |
| 校验结果 | {{RESULT}} |

---

## 2. 预处理（preprocessing）

> 预处理是**第二高发**的静默分歧源。它不报错、不崩、只是让数字差一两个点。

| 检查项 | 论文 | 本次 | 一致？ | 证据 / 代码位置 |
|---|---|---|---|---|
| resize / crop 尺寸 | {{VALUE}} | {{VALUE}} | | `{{FILE}}:{{LINE}}` |
| resize 插值方式（bilinear / bicubic） | {{VALUE}} | {{VALUE}} | | |
| 归一化 mean / std | {{VALUE}} | {{VALUE}} | | |
| 归一化前的数值范围（0-1 / 0-255） | {{VALUE}} | {{VALUE}} | | |
| 训练时增强（augmentation） | {{LIST}} | {{LIST}} | | |
| 测试时是否用增强 | {{VALUE}} | {{VALUE}} | | |
| 采样帧率 / 抽帧策略 | {{VALUE}} | {{VALUE}} | | |
| 时序数据的 scaler（standard / minmax） | {{VALUE}} | {{VALUE}} | | |
| scaler 的 fit 范围（train only？） | {{VALUE}} | {{VALUE}} | | ⚠️ fit 到全量 = 泄漏 |
| 其他 | | | | |

**必做的验证**：把同一份原始样本喂给「论文的实现」与「我的实现」，比对输出的张量。

```python
# 建议做法：取 N=8 个训练样本，两种预处理各跑一遍，比较
# - tensor shape 是否一致
# - max abs diff 是否在 1e-6 量级（不要求逐位相同，但要能解释差异来源）
```

| 样本 | shape 一致 | max\|Δ\| | 差异来源 |
|---|---|---|---|
| {{SAMPLE_1}} | ✅/❌ | {{DIFF}} | {{REASON}} |

> 若无法运行论文实现（无代码 / 依赖装不上），**在 §8「未对齐项汇总」显著标注「预处理未经逐样本比对」**（并在 §6 的 gap 归因表里把它列为未排除项）——这是一个真实的、必须传递下去的不确定性。

---

## 3. 评测指标实现 ⚠️ 最高优先

> **这是 #1 静默分歧源。**
> 指标算得不一样，数字就**不可比**——即使模型完全正确、训练完全复现。

同一份预测结果，用不同实现算出的指标可以有**可观测的差距**，而且通常**不会报错**。

| 指标 | 论文实现来源 | 本次实现来源 | 已比对？ | 证据 |
|---|---|---|---|---|
| {{METRIC_1}} (e.g. top-1 acc) | {{PAPER_IMPL}} | {{MINE_IMPL}} | ✅/❌ | {{EVIDENCE}} |
| {{METRIC_2}} (e.g. MSE / MAE) | | | | |
| {{METRIC_3}} (e.g. F1 / mAP) | | | | |

**逐项确认以下易分歧点**：

| 分歧点 | 论文 | 本次 | 一致？ |
|---|---|---|---|
| 是 percentage（0-100）还是 fraction（0-1） | {{VALUE}} | {{VALUE}} | |
| 平均方式：macro / micro / weighted | {{VALUE}} | {{VALUE}} | |
| 是 all-sample 平均还是 per-batch 平均再平均 | {{VALUE}} | {{VALUE}} | ⚠️ 不等价 |
| 多类时的 ignore_index / 忽略类别 | {{VALUE}} | {{VALUE}} | |
| 多 clip / 多 crop 的聚合方式（平均 logits vs 平均 prob vs 多数投票） | {{VALUE}} | {{VALUE}} | ⚠️ 不等价 |
| 数值精度 / 是否中途 round | {{VALUE}} | {{VALUE}} | |
| 是否 reported best-epoch vs last-epoch | {{VALUE}} | {{VALUE}} | |
| 是否在 test 前做了模型选择（用 val 挑 epoch） | {{VALUE}} | {{VALUE}} | ⚠️ 不等价 |

**强制验证（"指标实现的单元测试"）**：

> 拿**论文作者发布的预测结果**或**官方 checkpoint 的输出**，用你的指标代码算一遍，看是否得到论文报告值。
> 若没有官方 checkpoint，退而求其次：构造一个**手工可算的小例子**（如 10 个样本、已知正确答案），确认你的实现给出预期值。

| 验证方式 | 输入 | 期望 | 实测 | 结论 |
|---|---|---|---|---|
| {{METHOD}} | {{INPUT}} | {{EXPECTED}} | {{ACTUAL}} | {{VERDICT}} |

`[TODO: 若论文有官方 ckpt，必须做这一项——它是区分「训练错」与「评测错」的唯一手段]`

---

## 4. 随机种子处理（seed handling）

> 判断规则：**seed 消除无关变异，repeat 度量有关变异。**

| 随机性来源 | 是实验变量还是噪声？ | 处理方式 | 具体取值 |
|---|---|---|---|
| 模型初始化 | {{VAR_OR_NOISE}} | pin / repeat | {{SEED}} |
| 数据 shuffle | 噪声 | pin | {{SEED}} |
| 数据 split | {{VAR_OR_NOISE}} | pin / repeat | {{SEED}} |
| dropout | 噪声 | pin | {{SEED}} |
| 随机掩码 / 随机采样（**若为实验变量**） | **变量** | **repeat N≥3，不 pin** | seeds = {{LIST}} |
| 数据增强随机性 | {{VAR_OR_NOISE}} | pin / repeat | {{SEED}} |
| 对照组 / baseline | 参照 | **pin，全组共用** | {{SEED}} |
| 论文是否报告 seed | {{YES_NO}} | — | {{PAPER_SEED_IF_ANY}} |

**本次的重复策略**：

| 条件 | N (repeats) | seed 策略 | 记录格式 |
|---|---|---|---|
| {{CONDITION_1}} | 3 | 每 rep 不同 seed | per-rep raw + mean±std |
| {{CONTROL}} | 1 | 固定 seed | 单值 |

**实测方差**（用于设定成功阈值）：

```
condition: {{CONDITION}}
reps      = [{{R1}}, {{R2}}, {{R3}}]
mean ± std = {{MEAN}} ± {{STD}}
```

> `[TODO: 用这个实测 std 回填 docs/reproduction_protocol.md §6 的阈值——不要用拍脑袋的阈值]`

---

## 5. 硬件与精度影响

| 检查项 | 论文 | 本次 | 一致？ | 备注 |
|---|---|---|---|---|
| GPU 型号 | {{PAPER}} | {{MINE}} | | |
| GPU 数量 | {{NUM}} | {{NUM}} | | |
| 单卡 batch size | {{NUM}} | {{NUM}} | | |
| 等效总 batch（含梯度累积） | {{NUM}} | {{NUM}} | | ⚠️ 影响 lr 有效性 |
| 训练精度 | [TODO: 论文用的精度（fp32 / fp16 / bf16）] | {{VALUE}} | | |
| 是否用 AMP | {{YES_NO}} | {{YES_NO}} | | |
| 是否用 TF32 | {{YES_NO}} | {{YES_NO}} | | ⚠️ 默认开通常是隐患 |
| 分布式后端（DDP / FSDP / DeepSpeed） | {{VALUE}} | {{VALUE}} | | |
| 算子确定性（`torch.use_deterministic_algorithms`） | {{VALUE}} | {{VALUE}} | | |
| cudnn benchmark | {{VALUE}} | {{VALUE}} | | |
| CUDA / PyTorch / 关键库版本 | {{VERSIONS}} | {{VERSIONS}} | | |

**精度差异的可接受性判断**：

- `fp32 → bf16/amp`：通常带来一定幅度的波动，[TODO: 用自己的 repeat 实验实测其量级并填入]
- 换 GPU 型号（**示例**：`A100 → 3090`）：主要影响速度与可用的 batch size，一般认为数值上无系统偏差——但这是**经验判断、不是本项目已证结论**，自己换机器时仍要用重复实验确认
- **TF32 默认开启**是常见隐患：矩阵乘法精度下降，可能导致**难以归因的**小幅掉点 → 显式关闭或显式记录

**若精度/硬件不一致**：在 `docs/reproduction_protocol.md` §4 登记为偏离，并标注「预期影响 ±[TODO: 量级]」。

---

## 6. Baseline 对照表 ⭐

> 本文件最终要产出的东西。**每次正式运行后更新**。

| # | 指标 | 论文报告值 | 论文出处 | 我的实测 mean±std | n | Δ (mine − paper) | 相对差 | 解释 / 归因 |
|---|---|---|---|---|---|---|---|---|
| 1 | {{METRIC}} | {{PAPER}} | {{SECTION_TABLE}} | {{MINE}} | {{N}} | {{DELTA}} | {{PCT}} | {{EXPLANATION}} |
| 2 | | | | | | | | |
| 3 | | | | | | | | |

**Δ 的解读指引**：

| 情形 | 优先怀疑 | 处置 |
|---|---|---|
| \|Δ\| ≤ 实测 std | seed 方差 | 无需动作，记录即可 |
| Δ < 0（我更低），同 setup 下稳定复现 | 实现细节 / 未登记偏离 | 走 `docs/reproduction_protocol.md` §7 的 L3 |
| Δ > 0（我更高）且稳定 | **评测实现可疑 / 数据泄漏** | 先查 §3，不要庆祝 |
| Δ 随规模变化（小规模对得上、大规模对不上） | 训练不充分 / 超参未对齐 | 查 lr、epochs、warmup |

**gap 归因表**（逐假设排除，不要并列陈述）：

| 假设 | 检验方法 | 结论 | 排除？ |
|---|---|---|---|
| 评测实现差异 | §3 的强制验证 | {{RESULT}} | ✅/❌ |
| 数据 split 不一致 | §1 样本数比对 | {{RESULT}} | |
| 预处理差异 | §2 逐样本比对 | {{RESULT}} | |
| 精度 / 硬件 | §5 | {{RESULT}} | |
| seed 方差 | §4 实测 std | {{RESULT}} | |
| 训练不充分 | {{METHOD}} | {{RESULT}} | |

---

## 7. 环境变更记录

| 日期 | 变更 | 影响的检查项 | 是否重跑本表 | 新旧结果是否可比 |
|---|---|---|---|---|
| YYYY-MM-DD | {{CHANGE}} | {{SECTIONS}} | {{YES_NO}} | {{YES_NO}} |

> **换环境 = 换实验条件。** 环境变更前后跑出的数字**不能**放进同一张结果表而毫无说明。

---

## 8. 未对齐项汇总（必须显式声明）

> 本文件的诚实性出口。跑完仍存在的未对齐项写在这里，并同步到 session log 与最终结论。

| # | 未对齐项 | 为什么没对齐 | 已知影响 | 对结论的威胁程度 |
|---|---|---|---|---|
| U1 | {{ITEM}} | {{WHY}} | {{IMPACT}} | 高 / 中 / 低 |
| U2 | | | | |

**一句话总结**：

> 在 {{ALIGNED_ITEMS}} 已对齐、{{UNALIGNED_ITEMS}} 未对齐的条件下，本实现相对论文的差距为 {{DELTA}}。
