# 复现方案：{{PAPER_TITLE}}

> 本文件是**一篇论文一份**的复现计划（replication protocol）。
> 在**动手之前**填写；执行中若有变更，就地修改并在「变更记录」追加一行（不要删旧值）。

**status**: `planning` | `running` | `done` | `abandoned`
**created**: [TODO: YYYY-MM-DD]
**updated**: [TODO: YYYY-MM-DD]
**owner**: {{WHO}}

---

## 1. 原论文信息

| 项 | 内容 |
|---|---|
| 标题 | {{PAPER_TITLE}} |
| 作者 / 机构 | {{AUTHORS}} |
| 出处（venue + year） | {{VENUE_YEAR}} |
| 链接（arXiv / DOI / 会议页） | {{URL}} |
| 官方代码仓库 | {{CODE_REPO}} |
| 仓库 commit / 版本 | {{COMMIT}} |
| 是否有官方 checkpoint | {{YES_NO_WHICH}} |
| 是否有第三方复现可参考 | {{REPRO_LINKS}} |
| 论文版本（v1 / v2 / camera-ready） | {{VERSION}} |

> `[TODO: 确认是否有 v2 与 camera-ready 的数值差异——会议版常补实验改数字]`

---

## 2. 复现目标

**等级**（三选一，勾选后其余作废）：

- [ ] **严格复现（strict）**：参数、数据、预处理、评测全部按论文，目标是数字落入判据区间。
- [ ] **近似复现（approximate）**：接受已登记的偏离，目标是在此条件下取得**同量级**结果并解释 gap。
- [ ] **只复现某几张表 / 图（partial）**：只针对下列条目，其余明确不复现。

**本次复现的具体条目**：

| 目标 | 论文中的位置 | 优先级 | 备注 |
|---|---|---|---|
| {{TABLE_1}} | {{SECTION_OR_TABLE}} | P0 | {{NOTE}} |
| {{TABLE_2}} | {{SECTION_OR_TABLE}} | P1 | {{NOTE}} |

**明确不复现的部分**（写清楚才不会被追问）：

1. ❌ {{NOT_REPRODUCING_1}}
2. ❌ {{NOT_REPRODUCING_2}}

**复现目的**（决定判据松紧）：

- [ ] 为后续改进建立可信 baseline（→ 判据严，必须能解释 gap）
- [ ] 验证方法在自己数据/任务上是否成立（→ 判据可松，重点在趋势）
- [ ] 纯粹学习实现细节（→ 判据最松，重点是记录）

---

## 3. 原论文关键参数逐条对照表

> **本表是本文件的核心。** 每一行都必须填「出处」——填不出出处的参数，说明你还没真正确认它在论文里的值。
> 「本次取值」列在执行前填计划值，执行后如与计划不符**另起一行**记录，不要覆盖。

### 3.1 模型结构

| 参数 | 论文值 | 出处（论文哪一节 / 哪张表 / 哪行） | 本次取值 | 若不同则原因 |
|---|---|---|---|---|
| backbone | {{VALUE}} | {{SECTION}} | {{VALUE}} | [TODO: 若不同写明原因；与论文相同则显式写 N/A] |
| 层数 / hidden dim / heads | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| patch size | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| 参数量 | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| 特殊结构（如非对称 encoder-decoder） | {{VALUE}} | {{SECTION}} | {{VALUE}} | |

### 3.2 预训练设置

| 参数 | 论文值 | 出处 | 本次取值 | 若不同则原因 |
|---|---|---|---|---|
| 预训练数据 | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| 输入尺寸 / clip 长度 | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| masking ratio | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| masking 策略 | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| optimizer / lr / weight decay | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| lr schedule / warmup | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| batch size | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| epochs / iterations | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| 精度（fp32 / fp16 / bf16） | {{VALUE}} | {{SECTION}} | {{VALUE}} | |

### 3.3 下游微调 / 评测设置

| 参数 | 论文值 | 出处 | 本次取值 | 若不同则原因 |
|---|---|---|---|---|
| 下游数据集 + split | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| 微调协议（full ft / linear probe / 冻结层数） | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| 微调 epochs / lr / layer-wise decay | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| 评测指标实现 | {{VALUE}} | {{SECTION}} | {{VALUE}} | |
| 测试增强（多 clip / 多 crop） | {{VALUE}} | {{SECTION}} | {{VALUE}} | |

### 3.4 论文中**未说明**但必须自行决定的参数 ⚠️

> 这一类是失败模式 (a) 的高发区：论文不提，你随手选了一个，然后与论文数值对不上。
> 写下来，并在结果里标注「此参数为论文未说明，本次取值 X」。

| 参数 | 论文是否说明 | 本次取值 | 依据 / 理由 |
|---|---|---|---|
| [TODO: 如 seed 策略] | [TODO: 核对论文后填 ✅ 已说明 / ❌ 未说明] | [TODO: 本次取值] | [TODO: 为什么这么选] |
| [TODO: 如 dataloader workers] | [TODO: ✅/❌] | [TODO] | [TODO] |
| [TODO: 如 是否用 EMA] | [TODO: ✅/❌] | [TODO] | [TODO] |

---

## 4. 允许的偏离清单（deviations）

> **在开跑前登记。** 每条偏离必须写明「为什么必须偏离」与「预期它对结果的影响方向和量级」。
> 无法评估影响的，写「影响未知」——**未知也是信息**，比假装无害强。

| # | 偏离内容 | 相对论文的哪一条 | 为什么必须偏离 | 预期影响 | 已验证影响？ |
|---|---|---|---|---|---|
| [TODO: 如 D1] | [TODO: 偏离内容，如「用 4 卡代替论文的 8 卡」/「换用不同型号的 GPU」] | [TODO: §X 的哪一条] | [TODO: 为什么必须偏离] | [TODO: 预期影响方向与量级] | [TODO] |
| [TODO: D2] | [TODO] | [TODO] | [TODO] | [TODO] | [TODO] |

**不允许的偏离**（红线，越线即不叫复现）：

1. ❌ 改动模型结构 / 损失函数
2. ❌ 改动数据 split
3. ❌ 在评测中使用论文未使用的额外数据

**未登记偏离的处理**：执行中发现必须新增偏离时，**停下当前 run**，先登记再继续。跑完才发现未登记的偏离，该 run 的结果只能标注为「条件未知」，不得用于对论文的比较。

---

## 5. 计算预算与时间预估

| 阶段 | 单次耗时估计 | 重复数 N | 总耗时 | 备注 |
|---|---|---|---|---|
| 环境搭建 + 代码跑通 | {{HOURS}} | 1 | {{SUM}} | 最大不确定性来源 |
| baseline 对齐（小规模 sanity check） | {{HOURS}} | 1 | {{SUM}} | **不要跳过** |
| 预训练 | {{HOURS}} | {{N}} | {{SUM}} | |
| 下游微调 + 评测 | {{HOURS}} | {{N}} | {{SUM}} | |
| **合计** | | | **{{TOTAL}}** | |

**资源**：{{GPU_MODEL}} × {{COUNT}}，{{HOURS_PER_DAY}} h/day → 预计 {{DAYS}} 天

**预算超支的处置**（事先定好，避免中途犹豫）：
- 超出 {{THRESHOLD}} → 降级为近似复现，砍掉 {{WHICH}} 条目
- 超出 {{THRESHOLD_2}} → 触发第 7 节降级方案

---

## 6. 成功判据

> **必须是数值，不是形容词。** 「差不多」「趋势一致」不是判据。

| 指标 | 论文报告值 | 我的成功阈值 | 判据类型 | 说明 |
|---|---|---|---|---|
| {{METRIC_1}} | {{PAPER_NUM}} | ±{{DELTA}} 以内 | 绝对差 | {{NOTE}} |
| {{METRIC_2}} | {{PAPER_NUM}} | ±{{DELTA_PCT}}% 以内 | 相对差 | {{NOTE}} |

**判据设定依据**：
- 阈值来源：`[TODO: 若论文有方差报告则用之；否则参考同规模任务的典型 seed 方差]`
- 我的 baseline 实测方差（见 `docs/baseline_alignment.md`）：±{{MEASURED_STD}}

**分级结论**（事先定义，避免事后找理由）：

| 结果 | 判定 |
|---|---|
| 落入阈值内 | ✅ **复现成功**——可在此基础上做改进 |
| 超出 1× 阈值但同号、趋势一致 | ⚠️ **部分复现**——必须定位到具体偏离项并说明 |
| 超出 2× 阈值 | ❌ **复现失败**——触发第 7 节，且**不得**用「硬件差异」一句带过 |
| 优于论文报告值 | ⚠️ **可疑**——优先怀疑评测实现有误 / 数据泄漏，而非自己更强 |

> 最后一条尤其重要：**复现结果显著优于论文，通常意味着你搞错了什么地方**，而不是你复现得好。

---

## 7. 失败时的降级方案

按顺序尝试，每步记入 session log：

| 层级 | 动作 | 预期代价 | 触发条件 |
|---|---|---|---|
| L1 | 用**官方 checkpoint** 跑一遍评测 → 隔离「训练问题」还是「评测问题」 | {{HOURS}} | 首次复现失败 |
| L2 | 缩小规模（小数据子集 / 少 epochs）跑通全流程，确认 pipeline 无 bug | {{HOURS}} | L1 无法区分 |
| L3 | 逐项对齐实验：固定其他一切，只扫某一个可疑参数 | {{HOURS}} | L2 通过但主实验仍失败 |
| L4 | 联系作者 / 提 issue / 找第三方复现对照 | {{DAYS}} | L3 无效 |
| L5 | **降级为近似复现**：放弃对齐具体数值，改为「在已登记偏离条件下报告本实现的结果」并**明确声明不对齐论文** | — | 预算耗尽 |

**兜底原则**：**不允许无限期追一个数字。** 到 L5 时，产出物从「复现结果」变为「本条件下的独立结果 + gap 分析」，这仍然是有价值的交付物，但**必须在文档显著位置声明它不是复现**。

---

## 8. 结果记录位置

| 内容 | 写在哪 |
|---|---|
| 每次运行的原始数值（per-rep + mean±std） | `agent_memory/session_logs/session_YYYYMMDD_*.md` |
| baseline 差异对照 | `docs/baseline_alignment.md` |
| 最终复现结论 | 本文件 §9 |
| 复现产生的代码 / 配置 | {{CODE_PATH}} |

---

## 9. 复现结论（执行后填写）

**最终判定**：{{SUCCESS_PARTIAL_FAIL}}

| 指标 | 论文值 | 本次 mean±std | n | Δ | 判定 |
|---|---|---|---|---|---|
| {{METRIC}} | {{PAPER}} | {{MINE}} | {{N}} | {{DELTA}} | {{VERDICT}} |

**gap 归因**（逐项排除，不是猜）：

| 假设 | 检验方法 | 结论 |
|---|---|---|
| 评测实现差异 | 用官方 ckpt 跑同一评测代码 | {{RESULT}} |
| 数据 split 不一致 | 核对样本数 / 类别分布 | {{RESULT}} |
| 未登记偏离 | 逐条走 §4 | {{RESULT}} |
| seed 方差 | 见 §6 实测方差 | {{RESULT}} |

**本次复现的适用边界**（写清楚结论在什么条件下成立）：

- 在 {{CONDITIONS}} 下，本实现 {{MATCHES_DOES_NOT_MATCH}} 论文报告值。

---

## 10. 变更记录

| 日期 | 变更 | 原因 |
|---|---|---|
| YYYY-MM-DD | {{CHANGE}} | {{WHY}} |
