# cloud/ — 远程 GPU 实验

> 本地 Agent 负责**打包与归档**，云端 Agent 负责**执行与监督**，用户负责**上传与下载**。
> 一句话：**本地准备一个自解释的实验包 → 云端自主跑完并实时汇报 → 返回包核验后归档。**

**完整流程**：`cloud/docs/cloud_coordination.md`（可移植 Playbook，相对项目根）。
要在项目里补充专有约定（历史包记录、本项目特有的坑），在 `agent_memory/` 下新建
`cloud_coordination.md` 并在 `current_state.md` §10 登记——**本文件不预设它存在**。

---

## 目录职责

| 目录 | 角色 | 是否按日期归档 |
|---|---|---|
| `archive/` | **已完成实验的归档**，按 `{YYYY-MM-DD}/{实验名}/` 组织 | ✅ 是 |
| `package/` | **打包暂存区**，构建下一次上传包用 | ❌ 常驻，不归档 |
| `workflow/` | **常驻工具**：启动 Prompt 集 + 仪表盘模板 | ❌ 常驻，不归档 |
| `docs/` | 流程文档 | ❌ 常驻 |

归档规则见 `archive/README.md`。

> ⚠️ `package/` 下的 `README_FIRST.md` / `CLOUD_AGENTS.md` 是**写给云端 Agent 看的**，
> 描述的是**打包并解压之后**的状态。在暂存区里直接读到它们时，别把
> 「本压缩包是自包含的」当成对当前目录的描述——现在这里还只是个准备中的目录。
>
> 同理，`CLOUD_AGENTS.md` §2.1 那张「必备文件清单」是给**打包后**的云端 Agent 用的：
> 在暂存区里跑它必然报缺失（`RESEARCH_BACKGROUND.md`、`requirements.txt`、矩阵 CSV 等
> 都要在打包时才复制进来）。**别把暂存区的缺失当成打包缺陷。**

## 常用操作

```bash
# 核验返回包完整性（★必做，别急着关云端实例）
python cloud/verify_integrity.py --root <返回包解压目录> --zip <return_package>.zip

# 启动实时监督仪表盘（在云端实例上）
python3 cloud/workflow/dashboard_template/server.py \
    --root <实验输出目录> --logs <日志目录> --matrix <矩阵csv> --port 8501
```

## 三条容易踩的坑

1. **返回包必须包含「实际运行过的脚本」。** 云端 Agent 会现场修 bug——那些修补是最有价值的长期资产，
   要回写进 `package/` 暂存区，而不是只留在返回包里。
2. **同一次实验的全部格子必须跑在同一台实例上。** 跨实例有批次效应（经验值：同一格子在不同实例上可差 0.0034。**来自同类项目，非本项目数据**），
   会污染格子之间的横向比较。
3. **不要在返回包核验通过前关闭实例。** 同类项目出现过「数据完整但云端修复版脚本漏打包」的情况，
   需要让云端 Agent 补发。

## 平台

以 **AutoDL** 为例（不持久化、手动上传/下载、SSH + 自定义服务端口发布）。
换平台只需改 `package/README_FIRST.md` 里的上传与观测那一节，流程本身不变。
