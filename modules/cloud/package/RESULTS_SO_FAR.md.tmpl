# RESULTS SO FAR — 本地已有结果（供云端 Agent 参考）

> ⚠️ **若你看到本行，说明本文档尚未填写（第 1 节仍是空的）。**
> 那就是"本地没有可依赖的已有结果"，**不要**把下面的空表格当成基线。
> 你的任务应改为：照 `EXPERIMENT_PROTOCOL.md` 独立执行并如实报告，不做对照。
> —— 本地 Agent 填完第 1 节后，请删除本段落。

> **这份文档的用途**：让云端 Agent 知道**当前证据强度**——它才能判断「我这次跑出来算好还是算坏」。
> 只给任务不给已有结果的后果：重复验证已知结论，或把一次偏差当成重大发现。
>
> **滚动更新**：每次云端实验返回并分析后，本地 Agent 把新结论写回**本文件**
> （它在 `cloud/package/` 暂存区里，**下次打包时自动带上去**）。
> 最后更新：{{DATE}}

---

## 1. 已确认的结论（可直接依赖）

<!-- 每条写：结论 + 支撑数字 + 数据在哪。数字必须来自 results/ 下的文件，不要凭记忆写。 -->

| # | 结论 | 关键数字 | 数据来源 |
|---|---|---|---|
| 1 | [TODO: 结论一句话] | [TODO: 如 <指标> = <均值> ± <标准差>（n=3）] | [TODO: `results/.../summary.csv`] |
| 2 | [TODO] | [TODO] | [TODO] |

---

## 2. 已有实验记录

| 日期 | 实验 | 规模 | 关键结果 | 归档 |
|---|---|---|---|---|
| YYYY-MM-DD | [TODO: 实验名] | [TODO: N 格 × 3 重复] | [TODO: 最优格 + mean±std] | `cloud/archive/YYYY-MM-DD/[TODO: 实验名]/` |

---

## 3. 尚未解决 / 有争议的点

<!-- 这些是"为什么还要再跑一次云端"的理由，也是云端 Agent 判断异常时最重要的上下文。 -->

- [TODO: 例如「某格在两个实例上差 <跨实例批次变异量级，见 cloud/docs/cloud_coordination.md §9 的经验值——**非本项目数据**>，
  怀疑是批次变异 → 本次全量单机重跑消除」]
- [TODO: 例如「参数固化假说 vs 方向效应，尚未定论」]

---

## 4. 本次实验要检验什么

<!-- 与 EXPERIMENT_PROTOCOL.md §0 呼应，但这里是"从证据强度出发"的说法。 -->

- **要检验的论断**：[TODO]
- **判据（写成数值）**：[TODO: 例如「若 <条件> 的 mean±std 区间与 <对照> 不重叠，则判定为真」]
- **本次结果的预期位置**：[TODO: 例如「<对照条件> 上次排在 <名次>/<总数>，本次若仍在中游则支持 <假说>」]

---

## 5. 记录格式约定

写入任何数字时使用以下格式（per-rep 原始值**必须**保留）：

```
condition: [TODO: 条件标识]
n = 3
reps = [<值1>, <值2>, <值3>]          # per-rep 原始值，必须保留，不要只留均值
mean ± std = <均值> ± <标准差>
seed_policy: [TODO: 每 rep 不同 seed / 对照组 seed 固定为 0]
source: [TODO: 相对路径到 results.csv]
```

> **禁止**在本文件里写入任何**不是**从结果 CSV 读出来的数字（含"大约""估计"）。
