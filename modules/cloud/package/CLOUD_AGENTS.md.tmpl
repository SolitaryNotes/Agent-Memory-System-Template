# CLOUD AGENTS — 云端 Agent 自启动协议（★ 首先阅读本文档）

> 你是执行 **{{PROJECT_NAME}}** 云端实验的**云端 Agent**。
> 本压缩包是**自包含实验包**：解压后按本文档即可自主完成全部实验，无需与本地沟通。
> 你的最终产出是一个**返回压缩包**，由用户带回本地。
>
> **项目定位**：{{GOAL}}（领域：{{DOMAIN}}）

---

## 0. 你的任务（一句话）

<!-- 打包时由本地 Agent 填成一句可核对的话：做什么、做多少、跑到什么程度算完。 -->
[TODO: 一句话任务：在 <方法/模型> 上跑 <矩阵规模> × <重复次数> 次重复，完成 <阶段 A> + <阶段 B>，并打包返回。]

> **本次实验的目的**（理解它，遇到意外时你才知道该怎么取舍）：
> [TODO: 一句话科学目的，例如：消除跨实例批次变异，保证矩阵内部同机可比。]

---

## 1. 启动顺序（必须按顺序读）

1. **本文档**（`CLOUD_AGENTS.md`）— 自启动协议（执行手册）
2. **`RESEARCH_BACKGROUND.md`** — 完整科研背景（动机、科学问题、术语表、理论张力）★**必读**
3. **`EXPERIMENT_PROTOCOL.md`** — 详细协议（配置、矩阵、重复次数、单机一致性、时间预估）
4. **`RESULTS_SO_FAR.md`** — 本地已有结果与数字（当前证据强度）
5. `requirements.txt` — 依赖

> **第 2 份不要跳过。** 它决定了你在遇到协议没写到的情况时能否自主决策：
> 只读协议的人遇到异常只能停下等指令；理解科研目的的人能自己判断「该重试、该跳过、还是该上报」。

---

## 2. 环境检查

```bash
cd cloud_package                      # 本压缩包解压后的顶层目录（固定名，勿改名）
nvidia-smi                            # 确认 GPU 型号与显存
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
pip install -r requirements.txt       # 缺啥装啥（torch 需与 CUDA 版本匹配，已装则跳过）
ls [TODO: 数据文件路径]                 # 确认数据就位
```

### 2.1 先确认包是完整的（★ 不要跳过）

以下文件**必须存在**。缺任何一个都说明**打包不完整**——
此时**不要自行补全、不要猜测其内容、不要"先跑起来再说"**，直接向用户报告缺了什么。

```bash
ls RESEARCH_BACKGROUND.md EXPERIMENT_PROTOCOL.md CLOUD_AGENTS.md RESULTS_SO_FAR.md requirements.txt
```

| 缺失的文件 | 后果 |
|---|---|
| `RESEARCH_BACKGROUND.md` | 你无法理解**为什么做**这件事，遇到异常就无法自主纠偏 |
| `EXPERIMENT_PROTOCOL.md` | 你不知道**怎么跑** |
| `RESULTS_SO_FAR.md` | 你不知道当前**证据强度**，可能把一次正常波动当成重大发现 |
| `requirements.txt` | 环境依赖未随包提供 |

本项目自检命令：`{{BOOT_CMD}}`

**环境检查的结果要如实记录**（GPU 型号是 `CLOUD_AGENT_NOTE.md` 的必填项）。
若 GPU 型号与协议里的时间预估基准不同，**据此重新估算总时长并告知用户**，不要默默开跑。

---

## 3. 执行实验（核心）

```bash
nohup {{ENTRY_CMD}} > run_main.log 2>&1 &
# 或用 screen/tmux —— 长实验一律挂后台，禁止前台跑
```

<!-- 打包时把下面的"脚本会自动做什么"补全，云端 Agent 才能判断中间产物是否正常 -->
脚本会自动完成：

1. [TODO: 读取实验矩阵 CSV，跳过 status=done 的格子]
2. [TODO: 每个格子的阶段 A / 阶段 B 及各自 epoch 数]
3. [TODO: 自动汇总 summary.csv（per-rep 原始值 + mean±std）]
4. [TODO: 关键节点写 dashboard_state.json，并把每步日志 tee 到结果目录]

### 3.1 单机一致性（★ 本次是否适用见 `EXPERIMENT_PROTOCOL.md`）

**若协议中标注"要求单机一致性"**：

- **全部格子必须在同一实例、同一 GPU、同一会话内连续跑完**，中途不得换实例/换卡/换环境。
- 若因故中断，**续跑必须回到同一实例**；已完成格子标 `done` 后重跑剩余。
- 代价（经验值，**非本项目数据**）：同一格子在两个实例上可差 0.0034，足以翻转"哪个配置最优"的结论。

**若协议中标注"不要求"**：可跨实例，但要在 `CLOUD_AGENT_NOTE.md` 里注明。

### 3.2 续跑（按矩阵状态，不按 checkpoint）

- 中断后**不要**"从最近 checkpoint 恢复并继续跑完整个矩阵"——那会重跑大量已完成的格子，而云端按小时计费。
- 正确做法：把已成功完成的格子标 `done`，**重跑入口命令**，脚本会跳过 `done` 的格子。
- checkpoint 只用于**单格快速重跑**（例如某格阶段 B 挂了，从该格阶段 A 的 ckpt 直接跑阶段 B）。

### 3.3 若某步骤失败

- 先看日志定位；常见修复：补装依赖、确认数据路径、调小 batch size 绕开 OOM。
- 修复后在 `CLOUD_AGENT_NOTE.md` 里**如实记录失败与调整**。
- 修不动就跳过该格并标 `failed` + 原因，**不要填 0，不要沿用上一次的值**。

---

## 4. 实时监督与展示（★ 硬性，形式自定）

> **硬性的是结果，不是形式**：用户必须在实验进行中**实时看到宏观进度与详细日志**两层。
> 载体/布局/指标/刷新节奏**由你根据本次实验自行决定**；`cloud/workflow/dashboard_template/`
> 是**参考风格/起点，不是必须照抄的规范**。

- 实验脚本在关键节点写 `dashboard_state.json`（当前格子/rep/阶段、done_runs、逐格 mean±std、错误），**原子写**。
- 起浏览器实时仪表盘**或等价展示**（参考 `cloud/workflow/dashboard_template/`：纯 stdlib + 单文件 HTML，零 CDN）。
- `nohup` 后台运行，并**告诉用户公网访问方式**：
  - **AutoDL 自定义服务**：控制台 → 更多 → 自定义服务 → 发布 8501 端口 → 得到公网 URL；
  - **SSH 端口转发**：`ssh -p <端口> root@<region>.autodl.com -L 8501:127.0.0.1:8501`。
- `tee` 每步日志；**定期用简洁中文向用户简报**（当前格子、已完成 mean±std、剩余 ETA）。
- **严禁编造数字**：所有汇报数字必须**从 `results.csv` / `dashboard_state.json` 读出**，
  不得手写、不得估一个"差不多"的值。

---

## 5. 实验完成后：打包返回

```bash
bash scripts/package_results.sh       # 生成 return_package_<时间戳>.zip
```

**返回包必须包含**：

| 内容 | 说明 |
|---|---|
| 结果目录 | `results.csv`（per-rep 原始值）+ `summary.csv`（mean±std）+ 全日志 + 调度记录 |
| checkpoint | 每格最佳 checkpoint |
| `manifest.json` | 路径 + size 清单（供本地 `verify_integrity.py` 对照） |
| **`cloud_scripts/`** | **★ 你在云端实际运行/修改过的全部脚本** |
| `CLOUD_AGENT_NOTE.md` | 云端说明（见 §5.1） |

### 5.1 补全 `CLOUD_AGENT_NOTE.md`（必做）

- 实际 GPU 型号、运行总时长、**实际重复次数**。
- **单机一致性确认**（是否全程同一实例；如换过机器，换的是哪几格）。
- **全部格子的 mean±std 表**（从 `summary.csv` 读出，不要手打）。
- 任何异常、参数调整、失败格子的原因。

### 5.2 为什么必须回传 `cloud_scripts/`

你在执行中**必然要改脚本**（修参数名 bug、绕 OOM、补路径、加进度上报）。这些改动是**最有价值的长期产物**：
本地会把你改过的脚本**合并回打包暂存区**，下一轮上传的包因此比这一轮更健壮。

如果只回结果、不回脚本，本地就无法复现这批数字是怎么跑出来的，下次还会从同样的 bug 开始。
**所以 `cloud_scripts/` 是强制项，不是可选项。**

---

## 6. 向用户汇报（关键数字）

```
- 全部格子的 mean±std（从 summary.csv 读出）
- 对照组 / baseline 结果
- 结论：[TODO: 本次要检验的具体论断] 成立 / 不成立
- 单机一致性：是 / 否
- 总时长: ? 小时
- 返回包: return_package_<时间戳>.zip
```

---

## 7. 重要提示

- **不要修改**本地科研项目文件；本包是独立副本。
- **不要**把任何东西上传/同步到外部（本实验只在本实例内完成）。
- 不理解科学目的时，回读 `RESEARCH_BACKGROUND.md`。
- 结果**如实报告**（包括失败）；**严禁编造数字**。
- 实验结束（打包完成后）**记得关掉仪表盘服务**，并把公网端口映射收回。
