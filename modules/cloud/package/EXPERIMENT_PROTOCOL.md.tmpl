# EXPERIMENT PROTOCOL — {{PROJECT_NAME}}：<!-- TODO: 实验名 -->

> 云端 Agent **按此协议执行**。所有参数/命令应已封装进包内脚本（`{{ENTRY_CMD}}`）。
> 本协议是**参数的唯一事实源**：脚本与协议不一致时，**以协议为准并如实记录**。

---

## 0. 实验设计（先理解范式）

<!-- 若实验分阶段（如"自监督预训练 + 线性探针"），把每个阶段的作用与"是否用标签"写清楚。
     云端 Agent 靠这张表判断中间产物是否正常。 -->

| 阶段 | 内容 | 作用 | 是否用标签 |
|---|---|---|---|
| **阶段 A**（[TODO: N] ep） | [TODO: 如掩码重建预训练] | 学习表征 | ❌ 无监督 |
| **阶段 B**（[TODO: N] ep） | [TODO: 如冻结 encoder + 线性探针] | **"考卷"**：衡量表征质量 | ✅ 有监督（仅评估） |

> [TODO: 一句话说明为什么要拆成两阶段，以及阶段 B 只训哪部分、隔离了什么效应]

---

## 1. 模型 / 环境配置

| 参数 | 值 | 说明 |
|---|---|---|
| [TODO: 关键结构参数] | [TODO] | [TODO: 出处——论文哪一节/哪张表，或本项目既定值] |
| [TODO: 序列/输入参数] | [TODO] | |
| batch_size / lr | [TODO] | |
| 阶段 A epochs | [TODO] | |
| 阶段 B epochs | [TODO] | |
| 随机性策略 | [TODO: 每 rep 不同 seed（repeat）/ 固定 seed（对照）] | 见 §4 |

---

## 2. 实验矩阵（本次核心）

- **矩阵规模**：[TODO: 如 3×3×3 = 27 格 + 1 个随机对照]
- **矩阵文件**：`[TODO: xxx_matrix.csv]`，列为 `[TODO: 条件字段..., status]`，**status ∈ {todo, done, failed}**。
- **本次是否复用本地已有结果**：[TODO: 全部 todo 不复用 / 复用 N 格，已标 done]
- **每格 × N 次重复**（N = [TODO: 3]）+ 对照组 × N。
- 输出目录：`[TODO: results_xxx/]`（`results.csv` per-rep + `summary.csv` mean±std + 全日志）。
- 命令：
  ```bash
  {{ENTRY_CMD}}                          # 全量
  {{ENTRY_CMD}} --repeats 2              # 时间极端紧张时才用（必须记录）
  ```

---

## 3. 单机一致性（★ 条件性硬要求）

<!-- 二选一，删掉另一个。不要含糊。 -->

**[TODO: 选项 A —— 要求]** 本次矩阵内格子会被**互相比较**，因此：

- 全部格子必须**在同一实例、同一 GPU、同一会话**内连续跑完。
- 中途**不得换实例 / 换卡 / 换环境**；若因故中断，**续跑必须回到同一实例**，已完成格子标 `done` 后重跑剩余。
- 用 `nohup` / `screen` / `tmux` 挂后台，避免 SSH 断连中断实验。
- 在 `CLOUD_AGENT_NOTE.md` 中显式确认「单机一致性：是」。
- 理由（经验值，**非本项目数据**）：同一格子在两个实例上可差 **0.0034**，而矩阵中"最优 vs 次优"的差距可能只有 0.002 量级。

**[TODO: 选项 B —— 不要求]** 本次不做格子间互相比较（只单独看绝对水平），**不要求**单机一致性；
换机器无需作废，但必须在 `CLOUD_AGENT_NOTE.md` 中注明哪些格子跑了在哪台机器上。

---

## 4. 重复与随机性

| 项 | 规定 |
|---|---|
| 重复次数 | **N = 3**（硬性默认） |
| 记录 | **per-rep 原始值** + **mean ± std**（只留均值是错误做法） |
| 降级 | 仅时间极端紧张时 N = 2，**必须**在 `CLOUD_AGENT_NOTE.md` 与归档 README 中记录「n=2，方差不可靠」 |
| 对照组 seed | **pin 死**（对照组必须每次都一样，否则失去参照意义） |
| 实验变量含随机性时 | **不 pin seed**，靠 repeat 度量波动；把随机性这个变量本身估计出来 |

> [TODO: 本实验里哪些随机性属于"实验变量"（→ repeat），哪些属于"噪声"（→ pin seed）]

---

## 5. 时间预估

| 方案 | runs | 预估 |
|---|---|---|
| 全量 × N=3 | [TODO] | **[TODO: 如 8–9 小时，建议过夜]** |
| 降 N=2 | [TODO] | [TODO] |

> 实测基准：[TODO: 填**本项目自己测出来的**数字，例如「~35 min/run（阶段 A ~25 min + 阶段 B ~10 min），
> GPU 型号 <本项目用的卡>」]。
> 下面这句只是**示例格式**，不是本项目的测量值：「~6 min/run（阶段 A ~4 min + 阶段 B ~2 min），GPU 型号 RTX 3090」。
> **不要照抄示例里的时长或显卡型号**——留空写 `[TODO]` 也好过填一个没测过的数。
> 若实际 GPU 与基准不同，**据此重新估算并告知用户**。

---

## 6. 实时监督（★ 硬性，形式自定）

按 `CLOUD_AGENTS.md` §4 执行：

- 实验脚本在关键节点写 `dashboard_state.json`（原子写），`tee` 每步日志。
- 启动实时展示（参考 `cloud/workflow/dashboard_template/`，**形式自定**——布局/指标/刷新节奏由云端 Agent 自行决定）。
- 定期向用户简报进度；失败如实说明并修复。
- **严禁编造数字**：所有汇报数字从 `results.csv` / `dashboard_state.json` 读出。

---

## 7. 续跑（按矩阵状态，不按 checkpoint）

- 中断后把已完成的格子标 `status=done`，**重跑入口命令**（脚本会跳过 done 的格子）。
- **不要**"从最近 checkpoint 恢复并继续跑完整个矩阵"——那会重跑大量已完成工作，而云端按小时计费。
- checkpoint 只用于**单格快速重跑**。

---

## 8. 完成后

```bash
bash scripts/package_results.sh        # → return_package_<时间戳>.zip
```

返回包必须包含：结果目录（per-rep + mean±std + 全日志）+ 每格最佳 checkpoint + `manifest.json`
+ **`cloud_scripts/`（云端实际运行/修改过的脚本）** + `CLOUD_AGENT_NOTE.md`（GPU / 总时长 /
重复次数 / 单机一致性确认 / 全部 mean±std / 异常）。

> `cloud_scripts/` 是**强制项**：本地会把你改过的脚本合并回打包暂存区，让下一轮上传的包更健壮。
