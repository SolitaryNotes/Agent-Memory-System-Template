# README FIRST — 用户使用说明（{{PROJECT_NAME}} 云端实验包）

> **这份文档是给你（人）看的，不是给 Agent 看的。**
> 本压缩包是**自包含云端实验包**：只上传这一个 zip 即可，云端 Agent 解压后自启动。
> 打包日期：**[TODO: 打包当天的日期]**（不要沿用模板建立日期）｜ 实验：**[TODO: 实验名]**

---

## 一、上传云端（AutoDL）

1. 把 `[TODO: 包名].zip` 上传到 AutoDL 实例（任意目录，JupyterLab 上传 / 网页上传 / scp 均可）。
2. 云端解压（**顶层目录名固定，命令是常数，不用查**）：
   ```bash
   unzip [TODO: 包名].zip
   cd cloud_package
   ```
3. 启动云端 Agent（Claude Code 或等同物），把你**本机**的 `cloud/workflow/CLOUD_START_PROMPTS.md`
   里对应的那一段 Prompt 原样贴给它。（该文件在本地项目里，**不在这个包里**——包内只需要
   云端 Agent 自己读的文档。）它会先读 `CLOUD_AGENTS.md`，然后按协议执行。

---

## 二、云端实验内容（概览）

- **[TODO: 实验名]**：[TODO: 一句话说清跑什么——矩阵规模 × 重复次数、两个阶段的 epoch 数]
- 关键配置：[TODO: 如 d_model / 序列长度 / batch size]
- **重复次数：N = [TODO: 3]**（[TODO: 3 是硬性默认；若降为 2 需说明原因]）
- **单机一致性**：[TODO: 要求 / 不要求]（[TODO: 要求时说明"全部格子在同一实例连续跑完，中途不得换机器"]）
- **预估耗时**：[TODO: 如 8–9 小时（建议过夜）]
- 入口命令：`{{ENTRY_CMD}}`

---

## 三、实时监督

云端 Agent 会启动**实时展示**（浏览器仪表盘 / 日志流 / 定期摘要，形式由它自定）+ 日志流，
**让你实时看到宏观进度与详细日志**。访问方式（AutoDL 自定义服务 URL 或 SSH 隧道命令）它会告诉你。

> 你随时可以问它「现在跑到哪了」，它应当能立刻给出：当前格子、已完成数、剩余 ETA。

---

## 四、取回结果

1. 云端 Agent 跑完后生成 `return_package_<时间戳>.zip`，**在实例上告诉你文件名**。
2. 把它下载回本地（JupyterLab 下载 / 网页下载 / scp）。
3. 把返回包交给本地 Agent：它会解压 → 跑 `cloud/verify_integrity.py` 核验 → 归档 → 同步结果。
4. **核验通过之前，先不要关闭云端实例**——实例关机即丢盘，缺的东西再也拿不回来。
   本地 Agent 核验完会明确告诉你「可以关机了」。

---

## 五、目录结构（包内）

```
cloud_package/
├── CLOUD_AGENTS.md              ← 云端 Agent 先读它（自启动协议）
├── README_FIRST.md              ← 本文件（给人看的）
├── RESEARCH_BACKGROUND.md       ← 完整科研背景（★必读）【打包时由项目背景文档复制而来】
├── EXPERIMENT_PROTOCOL.md       ← 详细协议（配置 / 矩阵 / 重复 / 时间预估）
├── RESULTS_SO_FAR.md            ← 本地已有结果（当前证据强度）
├── requirements.txt             ← 【打包时从项目根复制】
├── [TODO: 实验矩阵 CSV]          ← 含 status=todo/done，续跑按它跳过已完成格子
└── [TODO: 实验代码目录]/          ← 代码 + 内置数据 + scripts
    ├── [TODO: 主脚本]
    ├── scripts/                 ← 入口 / 打包 / 进度上报脚本
    └── dataset/                 ← 数据已内置，不依赖云端再下载
```

> **四份核心文档缺一不可。** 尤其 `RESEARCH_BACKGROUND.md`：它让云端 Agent **理解为什么做**，
> 从而在遇到协议没写到的情况时能自主判断"该重试、该跳过、还是该上报"。
> 只给"做什么"的 Agent 遇到异常只会停下来等指令。

---

## 六、注意事项

- **云端实例不持久** → 代码、数据、四份文档都已在包内，不假设云端有任何历史内容。
- **本包是副本**，本地源文件未改动；`cloud_package/` 这个顶层目录名是**约定**，解压后不要改名。
- **重复 3 次是硬性默认**；只有时间极端紧张才降为 2 次，且必须在 `CLOUD_AGENT_NOTE.md` 里如实记录。
- **单机一致性**是本次的[核心 / 非]要求：[TODO: 若为核心，写"全部格子须在同一实例跑完"]。
- 实验结束后**记得让云端 Agent 关掉仪表盘服务**并收回端口映射（不要对公网长期暴露）。
- 包内不含任何密钥/token；[TODO: 若含敏感数据，在此说明处理方式]。
