# 本地 Agent ⇄ 云端 Agent 协同实验流程 — {{PROJECT_NAME}}

> **本文件是 {{PROJECT_NAME}} 的云端协同流程手册**，与具体研究方向无关，可整份复制到其它项目。
> 适用形态：**非持久 GPU 租用实例**（本文以 **AutoDL** 为例），**本地无法远程控制云端**，
> 上传与取回全靠**用户手工搬运 zip**。
> 相关文件（本文件落地在 `cloud/docs/`，以下为相对本文件的链接）：
> [`../archive/README.md`](../archive/README.md)（归档规则）·
> [`../workflow/CLOUD_START_PROMPTS.md`](../workflow/CLOUD_START_PROMPTS.md)（启动 Prompt 集）·
> [`../workflow/dashboard_template/`](../workflow/dashboard_template/)（仪表盘参考实现）。
> **风格约定**：中文行文，技术术语与代码保留英文。

---

## 0. 角色与分工

| 角色 | 位置 | 职责 |
|------|------|------|
| **本地 Agent** | 本地电脑 | 打包实验包；接收返回包、核验完整性、同步/分析、归档 |
| **用户** | — | 把 zip 上传云端、把返回包带回本地；**在云端全程观察实验进度** |
| **云端 Agent** | 云端实例（AutoDL 等） | 解压后自启动；执行实验；**实时监督与展示进度**；打包返回 |

> **核心原则**：云端实例不持久、本地无法远程控制云端 → 一切靠**压缩包内的文档 + 云端 Agent 的自主能力**；
> 且云端 Agent 必须**让用户能实时看到进度**，**绝不允许静默后台跑完**。

---

## 1. 整体流程

```
[本地]                                  [云端]                              [本地]
1. 写四份核心文档                       6. 解压 → 按序读文档
2. 复制代码+数据 → 打包 zip  ──zip──→   7. 环境检查
3. 用户上传 zip + 交启动 Prompt          8. 执行实验（nohup/screen）
                                        9. ★实时监督：仪表盘 + 日志流（用户实时看）
                                       10. 打包 return_package_<ts>.zip ──→  12. 解压
                                                                    ←── 11. 用户带回     13. 核验 + 归档 + 同步记忆
```

---

## 2. 为什么是「打包」而不是「远程执行」

本地 Agent **不能** SSH 进云端、不能推送命令、不能读云端文件。所以整个协同的信息通道只有两条：

1. **去程**：一个 zip（命名形式为 `cloud_<项目slug>_<日期>.zip`，由 §3.3 的构建命令生成）
2. **回程**：一个 zip（`return_package_<时间戳>.zip`）

这意味着：**去程 zip 里没有的东西，云端 Agent 就永远不知道**。四份核心文档的存在理由，全部源于这一句。

---

## 3. 本地 Agent：打包准备

### 3.1 四份核心文档（缺一不可）

| 文档 | 内容 | 作用 | 缺了会怎样 |
|------|------|------|-----------|
| `CLOUD_AGENTS.md` | 自启动协议：启动顺序、环境检查、执行入口、失败处理、打包返回 | 云端 Agent 的**执行手册**（★必须第一个读） | 云端 Agent 不知道从哪开始，会先问一堆本可以写清的问题 |
| `RESEARCH_BACKGROUND.md` | 完整科研背景：动机、本次实验的科学问题（[TODO: 本次要回答什么]）、术语表、理论张力、已有发现、当前任务 | 让云端 Agent **理解为什么做** | **最关键的一份。** 只有「what」的 Agent 遇到意外只能停下等指令；理解「why」的 Agent 才能自己判断「这个失败该重试、该跳过、还是该上报」 |
| `EXPERIMENT_PROTOCOL.md` | 详细实验协议：配置、矩阵、参数、重复次数、时间预估 | 让云端 Agent **知道怎么做** | 参数靠猜；没有时间预估则中途放弃或无限拖 |
| `RESULTS_SO_FAR.md` | 本地已有结果与数字 | 让云端 Agent 知道**当前证据强度** | 重复劳动；且无法判断「我这次跑出来算好还是算坏」 |

> **`RESEARCH_BACKGROUND.md` 从哪来**：从项目的研究背景文档（`[TODO: 背景文档路径]`）复制并裁剪，
> 去掉与本次实验无关的章节，保留动机 / 科学问题 / 术语 / 已有发现。
> **术语表不要省**——远程 Agent 没有你们组会的上下文。

### 3.2 代码 + 数据

- 复制实验代码（**含数据**）到包内，**路径一律相对路径**（解压后直接可跑）。
- 数据**内置**（如 `dataset/xxx.csv`），不依赖云端再下载（云端可能无外网或很慢）。
- 附 `requirements.txt`（云端 Agent 缺啥装啥）。
- **代码里必须已接入进度上报**（写 `dashboard_state.json`）+ `tee` 日志——见 §6。
- **想把仪表盘参考实现给云端 Agent 用**，就得把它一并复制进包内（否则云端只有文字描述、
  拿不到那几个文件）。不复制也行——§6 只要求"用户能实时看到"，形式由云端 Agent 自定。

### 3.3 打包：zip 顶层目录是**硬约定**

```bash
# 暂存区在 cloud/package/，zip 内部顶层目录固定为 cloud_package/
python - <<'PY'
import zipfile, pathlib
src = pathlib.Path("cloud/package")
with zipfile.ZipFile("cloud_{{PROJECT_SLUG}}_{{DATE}}.zip", "w", zipfile.ZIP_DEFLATED) as z:
    for p in src.rglob("*"):
        if p.is_file():
            z.write(p, arcname="cloud_package/" + p.relative_to(src).as_posix())
PY
```

**硬约定**：zip 内顶层目录**永远是 `cloud_package/`**（不随项目变）。

理由：云端解压后的命令因此是一个**常数**：

```bash
unzip <任意包名>.zip && cd cloud_package
```

用户不必每次翻文档查「这次顶层目录叫什么」，启动 Prompt 也不必每次改。多项目共用同一套肌肉记忆。

### 3.4 打包纪律

- **只复制，不移动，不删除本地源文件。** 需要更新的文件用 `cp` 覆盖暂存区里的副本。
- **zip 是唯一上传物。** 不要额外上传散文件、不要上传整个仓库、不要上传 `.git/`。
- 不要在 zip 里放密钥、token、个人数据（`[TODO: 本项目的敏感项清单]`）。

### 3.5 归档

打包完成后（或返回包回来之后）按 [`../archive/README.md`](../archive/README.md)（= `cloud/archive/README.md`）归档到
`cloud/archive/{YYYY-MM-DD}/{kebab-实验名}/`。**同一天多跑一次就加 `_2` / `_3`。**

---

## 4. 用户上传 + 启动 Prompt

1. 用户把 zip 上传到云端实例（AutoDL 的 JupyterLab 上传 / 网页上传 / scp，任意目录）。
2. 解压：`unzip <包名>.zip && cd cloud_package`
3. 把**启动 Prompt** 原样交给云端 Agent（Claude Code 或等同物）。
   - 起手模板：`cloud/workflow/CLOUD_START_PROMPTS.md` 的**通用段**；
   - 本次实验的具体 Prompt：**追加**到同一文件的末尾（历史不修改）。

---

## 5. 云端 Agent：自启动执行

1. **按序读文档**：`CLOUD_AGENTS.md` → `RESEARCH_BACKGROUND.md` → `EXPERIMENT_PROTOCOL.md` → `RESULTS_SO_FAR.md`。
2. **环境检查**：`nvidia-smi`、`python -c "import torch; print(torch.__version__, torch.cuda.is_available())"`、
   补装依赖、确认数据就位。本项目自检命令：`{{BOOT_CMD}}`
3. **执行实验**：`{{ENTRY_CMD}}`，用 `nohup`/`screen`/`tmux` 挂后台。
4. **★实时监督与展示**（§6）——**硬性要求**。
5. **打包返回**（§11）。

---

## 6. 实时监督与展示（★硬性要求，实现形式自由）

> **硬性的是结果，不是形式。**
> 唯一不可协商的约束：**用户在实验进行中，必须能实时看到「宏观进度」与「详细日志」两层信息**。
> 至于用什么载体、什么布局、什么指标、多久刷新一次——**由云端 Agent 根据本次实验自行决定**。
> `cloud/workflow/dashboard_template/` 是**参考风格/起点，不是必须照抄的规范**。

### 6.1 两个层级

| 层级 | 载体（示例） | 内容 | 用户怎么看 |
|------|------|------|-----------|
| **宏观进度** | 浏览器实时仪表盘 | 任务总览、当前格子/重复、总进度条、已用时间/ETA、逐格结果表（mean±std 实时更新）、格子热力图 | 打开浏览器 URL |
| **详细日志** | 日志文件 + 实时流 | 每个 run 的完整 log、主执行 log、失败记录 | 仪表盘日志面板 / 直接 `tail -f` |

### 6.2 云端 Agent 必须做到

1. **实验脚本要"会汇报"**：关键节点写进度状态文件（`dashboard_state.json`）——开始、当前格子、当前 rep、
   阶段、epoch、已完成格子的 mean±std、已跑/总数、错误。
   > **写状态文件的必须是实验脚本本身**，不是 Agent 事后手填。这是 §7 那条"结构性机制"的一半。
2. **启动实时监督展示**：可参考 `cloud/workflow/dashboard_template/`（`nohup python3 server.py --root . --port 8501 &`），
   也可自行设计等价展示。**形式自定，实时可见为准。**
3. **告诉用户访问方式**：
   - **AutoDL 自定义服务**（推荐）：控制台 → 更多 → 自定义服务 → 发布 8501 端口 → 得到公网 URL。
   - **SSH 端口转发**：`ssh -p <端口> root@<region>.autodl.com -L 8501:127.0.0.1:8501` → 浏览器开 `http://localhost:8501`。
4. **日志实时监督**：主执行 `tee` 写日志 + 关键节点实时 echo；Agent 自己也要**定期盯日志**，异常立即处理并记录。
5. **定期向用户简报**：开始时报 URL；每完成若干格汇报一次 mean±std；失败时如实汇报 + 处理方案。
6. **对用户的提问负责**：用户问"现在跑到哪了"，要能立刻给出准确回答（当前格子、已完成数、剩余 ETA）。

### 6.3 仪表盘设计要求（若选择仪表盘这一形式）

- **精简但不丢信息**：一屏放下「总体进度 + 格子状态热力图 + 实时日志尾」，不堆砌装饰。
- **整洁清新**：浅色主题、系统字体、单强调色；状态用色语义化（待跑=灰、运行中=蓝、完成=按指标上色、本地已有=绿勾）。
- **实时性**：前端 3 秒级轮询 `/api/state` + `/api/matrix`；日志面板轮询 `/api/logs`。
- **离线可用**：**零 CDN、零外部依赖**（云端可能无外网）——单文件 HTML + Python stdlib 服务端。
- **参考而非规范**：以上仅为参考风格，可自由调整。

### 6.4 进度状态文件约定（`dashboard_state.json`）

```json
{
  "experiment": "[TODO: 本次实验名]",
  "started_ts": 1723260000, "elapsed_sec": 1234,
  "total_runs": "[TODO: 本次矩阵的格子数]", "done_runs": 0,
  "current": {"code": "<当前格子>", "rep": 1, "stage": "<阶段>", "epoch": 0},
  "results": {"<格子>": {"value": 0.0, "std": 0.0, "n": 3}},
  "notes": ["<格子> done: <mean> ± <std>", "..."]
}
```

> **以上是示例结构**（时间戳、键名都只为示意）：`value` 是**指标值的通用键名**，不是某个指标的
> 专名——你的指标（mse / accuracy / loss…）叫什么，就改 `progress.py` 与仪表盘前端里的这一个键；
> 三处（`progress.py` 的 `done`、`index.html` 的取值、本节）必须一致。
> 格子/阶段的**静态定义**（有哪些格、本地已跑与否）由**实验矩阵 CSV** 提供（`/api/matrix`）；
> 状态文件只承载**实时增量**，两者在仪表盘前端合并。
> 状态文件必须**原子写**（先写 `.tmp` 再 `rename`），否则仪表盘会读到半截 JSON。

---

## 7. 严禁编造数字 —— 以及保证它做不到的**结构性机制**

**规则**：云端 Agent 与本地 Agent **一律不得手写任何实验数字**。包括"大概 0.38 左右"这种。

**光靠"要求 Agent 诚实"是不够的**——压力之下（用户催、看起来快跑完了）模型会倾向于给出一个"合理"的数字。
所以本流程用**结构**来保证：

| 机制 | 做法 | 效果 |
|------|------|------|
| **数字的唯一来源是文件** | per-rep 原始值由实验脚本写入 `results.csv`；mean±std 与汇总表**在读取时由 CSV 现算** | 数字不是"谁说的"，是"从哪读的" |
| **展示层现算，不落手填值** | 仪表盘的逐格 mean±std 来自 `dashboard_state.json`（脚本写）；汇总页与最终报告从 `results.csv` 现算 | Agent 即使想说谎也没有"写入点" |
| **简报必须可溯源** | Agent 向用户简报时，引用格式是「格子 + mean±std + 来自哪个文件」 | 用户可以随手核对 |
| **本地以 CSV 为准** | 本地 Agent 核验时**以返回包里的 CSV 为唯一事实源**；Agent 口头/文档里的数字若与 CSV 不符，**以 CSV 为准并记录不一致** | 失真无法穿过交接面 |
| **失败如实上报** | 跑挂的格子写 `status=failed` + 原因，**不写 0、不写上一次的值** | "缺失"是一个合法状态，不是要被填满的空位 |

> **一句话**：如果一个数字不是**从 CSV 读出来算的**，它就不许出现在任何汇报里。
> 任何"Agent 手打但 CSV 里没有"的数字，按缺陷处理，不按笔误处理。

---

## 8. 重复实验：N=3 是硬性默认

| 项 | 规定 |
|---|---|
| 默认重复次数 | **N = 3**（每个条件） |
| 记录 | **per-rep 原始值**（不要只留均值）+ **mean ± std** |
| 降级 | 仅当时间极端紧张时降为 N=2，且**必须在 `CLOUD_AGENT_NOTE.md` 与归档 README 里显式记录**「n=2，方差估计不可靠」 |
| 对照组的 seed | **pin 死**（对照组必须每次都一样，否则失去参照意义） |

### 为什么是 repeat 而不是 pin seed

**seed 用来消除无关变异，repeat 用来度量有关变异。**

- 当**随机性本身就是实验变量**（随机掩码、随机采样策略）时，固定 seed 等于把"随机性"这个变量钉死在一个取值上——
  你测的是「**这个 seed 下好不好**」，不是「**这个策略好不好**」；A/B 差异里混进了"恰好抽到好掩码"的可能，且**无法估计**。
- 当随机性只是噪声（数据划分、dataloader shuffle）时，**pin seed** 消除它，让条件之间可比。
- **对照组 / baseline 永远 pin seed**：它的作用是固定参照。

判据与记录格式另见 `agent_memory/` 的会话记录约定，以及（若项目里挂了复现规范文档）
`docs/reproduction_protocol.md`。

> **只保留 mean±std 而丢掉 per-rep 原始值是常见错误。** 后期要换统计检验、剔除异常 rep、算中位数时，
> 原始值没了就只能重跑——而重跑要重新租机器。

---

## 9. 单机一致性（★条件性硬要求）

**触发条件**：**只要矩阵内的格子会被互相比较**（"哪个配置最优""哪一档掩码更好"），就适用。

| 要求 | 说明 |
|---|---|
| **同一实例、同一 GPU、同一会话连续跑完** | 中途不得换实例 / 换卡 / 换环境 |
| **续跑必须回到同一实例** | 中断后续跑若换了机器，前面的格子作废（至少要在归档 README 里标注为"跨机，不可直接比较"） |
| **在归档 README 与 `CLOUD_AGENT_NOTE.md` 中显式确认** | 写一句「单机一致性：是/否」 |

**真实代价**（经验值，**非本项目数据**）：同类项目实测，**同一个格子（配置完全相同）在两个实例上跑出 0.0034 的差距**——
而该矩阵中"最优与次优"的差距本身就只有 0.002 量级。**跨实例的批次变异足以翻转结论。**

> 如果本次实验**不需要**格子间互相比较（例如只是单独跑一个新配置看绝对水平），可以不做单机约束，
> 但必须在 `EXPERIMENT_PROTOCOL.md` 里写明"本次不要求单机一致性"，避免云端 Agent 无谓地延长占用。

---

## 10. 中断与续跑：按**矩阵状态**，不按 checkpoint

**中断是常态**（SSH 断连、实例被回收、显存 OOM、磁盘满）。续跑的正确单位是**矩阵里的格子**，不是模型 checkpoint。

| 做法 | 说明 |
|---|---|
| **第一道防线：`nohup` / `screen` / `tmux`** | 抗 SSH 断连。长实验一律挂后台，禁止前台跑 |
| **矩阵 CSV 带 `status` 列** | `todo` / `done` / `failed`；脚本启动时读它，**跳过已 done 的格子** |
| **续跑 = 标记 + 重跑剩余** | 把已完成的格子标 `done`，然后**重跑整个脚本**（它会自动跳过 done 的） |
| **checkpoint 的定位** | checkpoint 只用于**单格快速重跑**（如某格阶段 B 挂了，从该格阶段 A 的 ckpt 直接跑阶段 B），**不是**矩阵级续跑机制 |
| **失败格子** | 标 `failed` + 记录原因；可以重试，但**不得写入编造的值** |

> **反模式**：把"续跑"实现成"从最近一次 checkpoint 恢复并继续跑完全部格子"。
> 那样会重跑大量已完成的工作，而云端是**按小时计费**的。

---

## 11. 云端 Agent：返回打包

返回包内容清单：

| 内容 | 路径示例 | 为什么必须 |
|---|---|---|
| **per-rep 原始结果** | `results.csv` | 唯一事实源；没有它，mean±std 无法被复核 |
| **汇总（mean±std）** | `summary.csv` | 由 CSV 现算，供快速读 |
| **完整日志** | `*.log`（每 run 的每个阶段一份）+ 主执行 log | 出问题时唯一能回溯的东西 |
| **每格最佳 checkpoint** | `checkpoints/*.pth` | 后续复用/单格重跑 |
| **`manifest.json`** | 根目录 | 路径 + size 清单，供本地 `verify_integrity.py` 对照 |
| **云端说明** | `CLOUD_AGENT_NOTE.md` | GPU 型号 / 总时长 / 重复次数 / **单机一致性确认** / 全部 mean±std / 异常与调整 |
| **★ `cloud_scripts/`** | 云端**实际运行**的脚本副本 | 见下 |

打包成 `return_package_<时间戳>.zip`。

### 11.1 为什么返回包必须含 `cloud_scripts/`（最容易漏、也最值钱的一项）

云端 Agent 在执行过程中**必然要改脚本**：修一个参数名 bug、绕过一次 OOM、补一个数据路径、
加一段进度上报。这些改动当时都"显然无害"，但如果**只有结果回来、脚本没回来**：

- 本地**无法复现**这批数字到底是怎么跑出来的；
- 下次打包又要从**同样的 bug** 开始（云端 Agent 会再修一遍，还可能修成不一样的方式）；
- **最值钱的产物丢了**——被打磨过的 harness 本身。

**正确做法**：

1. 云端 Agent 把所有被修改/新增的脚本复制到 `cloud_scripts/` 一并返回。
2. 本地 Agent 收到后，**把这些补丁合并回 `cloud/package/` 暂存区**（这是真正的改进闭环：
   下一轮上传的包比上一轮更健壮）。
3. 若补丁改变了协议语义（比如换了默认重复次数），**同步更新 `EXPERIMENT_PROTOCOL.md`**，
   并在归档 README 里记一句。

> **实测失效**（同类项目经历，**非本项目数据**）：曾经有一次返回包只带了结果、没带云端改过的脚本，只能靠日志反推当时跑的是什么。
> 因此（同类项目教训，**非本项目数据**），`cloud_scripts/` 在返回清单里是**强制项**，由 `verify_integrity.py` 检查其存在与非空。

---

## 12. 本地 Agent：接收、核验、归档、同步

### 12.1 核验清单（用户问"能不能关机"之前必须走完）

```bash
# 0) 解压
unzip return_package_<时间戳>.zip -d cloud/archive/{日期}/{实验名}/

# 1) 完整性核查（CRC + manifest 对照 + 每条件 N reps + checkpoint 计数 + cloud_scripts 非空）
python cloud/verify_integrity.py --root cloud/archive/{日期}/{实验名}/return_package_<时间戳> \
       --zip  cloud/archive/{日期}/{实验名}/return_package_<时间戳>.zip \
       --reps 3 --expect-scripts
```

逐项确认：

- [ ] **zip CRC 通过**（`testzip()` 无损坏项）
- [ ] **manifest 路径集合 vs 磁盘实际文件**——无缺失；多出来的文件已确认无害
- [ ] **每个条件的每个阶段都有全部 N 个 rep**（缺 rep 是最常见的静默损坏）
- [ ] **checkpoint 数量**与预期格子数一致
- [ ] **`cloud_scripts/` 非空**——★云端改过的脚本回来了（同类项目漏过一次，**非本项目数据**）
- [ ] `CLOUD_AGENT_NOTE.md` 里写明了**单机一致性**与**实际重复次数**
- [ ] 抽查 1–2 个格子的 `results.csv` 行数与 `summary.csv` 的 mean 对得上

### 12.2 然后才答复用户

**只有上述全部通过，才告诉用户可以关闭云端实例。** 若任一项不通过：

1. **不要关机**（关机即丢盘，缺的东西再也拿不回来）；
2. 列出缺失项，给出让云端 Agent 补发的最小操作（通常是把 `cloud_scripts/` 或漏掉的 checkpoint 重新打一个补发包）；
3. 若云端已无法恢复，**在归档 README 里显式记录缺什么**，并在结论里标注证据强度下降。

### 12.3 同步

1. 结果落盘到项目的 `results/`（目录规范见项目里的 `docs/experiment_protocol.md`，若已挂载）。
2. 更新记忆：`agent_memory/session_logs/` 写本次会话日志（含**全部产物路径**）+ `current_state.md` 更新索引。
3. 把结果回流进 `cloud/package/RESULTS_SO_FAR.md`——**下次打包时云端 Agent 就能看到**。
4. 把 `cloud_scripts/` 的补丁合并回 `cloud/package/` 暂存区（§11.1 第 2 步）。
5. 按 `cloud/archive/README.md` 补全日期 README（实验内容 / 本文件夹内容 / 关键结论 / 结果去向）。

---

## 13. 注意事项与坑

| 坑 | 对策 |
|---|---|
| **云端实例不持久** | 一切依赖 zip 自包含；不假设云端有历史代码/数据/环境 |
| **静默跑挂** | §6 实时监督是硬性要求；`nohup` 挂后台 + `tee` 日志 + 定期简报 |
| **云端 Agent 手打数字** | §7 结构性机制：所有展示数字从 CSV 现算 |
| **单机一致性被破坏** | §9；换实例后续跑 = 前面的格子作废 |
| **"续跑"退化成"从头跑"** | §10 矩阵状态驱动，不是 checkpoint 驱动 |
| **只回结果不回脚本** | §11.1 `cloud_scripts/` 强制项 |
| **端口/安全** | 仪表盘只监听实例内网或经 AutoDL 映射；**不要对公网裸暴露**；实验结束记得关服务 |
| **时间预估不写进协议** | 云端 Agent 会中途放弃或无限拖；`EXPERIMENT_PROTOCOL.md` 必须有耗时表 |
| **本地源文件被 `mv` 走** | §3.4 只复制不动源文件；归档也只用 `cp` |
| **同一天跑两次，文件互相覆盖** | §3.5 日期文件夹加 `_2`/`_3` 序号 |

---

## 14. 启动 Prompt（复制即用）

完整 Prompt 集见 `cloud/workflow/CLOUD_START_PROMPTS.md`（**只追加，不修改历史**）。本项目通用段：

```
你是"{{PROJECT_NAME}} · <实验名>"的云端实验 Agent。先读压缩包根目录的 CLOUD_AGENTS.md（自启动协议，
★必须第一个读），再依次读 RESEARCH_BACKGROUND.md、EXPERIMENT_PROTOCOL.md、RESULTS_SO_FAR.md，
充分理解科研目的（{{GOAL}}）与协议。然后按协议执行（{{ENTRY_CMD}}）。
执行期间必须做到【实时监督与展示】：(1) 实验脚本在关键节点写 dashboard_state.json 并 tee 每步日志；
(2) 启动实时监督展示——形式（浏览器仪表盘/日志流/定期摘要）由你根据本次实验自行设计（可参考
cloud/workflow/dashboard_template/），务必让我能实时看到宏观进度与详细日志，用 nohup 后台运行，
告诉我公网访问方式（AutoDL 自定义服务 URL 或 SSH 隧道命令）；(3) 定期用简洁中文向我简报进度
（当前格子、已完成 mean±std、剩余 ETA），失败时如实说明并尽量修复。
【单机一致性】<若需要则保留此段：全部格子必须在同一实例同一会话内连续跑完，中途不得换机器>。
严禁编造数字：所有数字必须从 results.csv / dashboard_state.json 读出，不得手写。
全部完成后运行打包脚本生成返回包，务必把云端实际运行的脚本复制进 cloud_scripts/，
并补全 CLOUD_AGENT_NOTE.md（GPU 型号、总时长、重复次数、单机一致性确认、全部 mean±std、异常），
最后告知我返回包文件名。
```
