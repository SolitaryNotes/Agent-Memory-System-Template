# 实验协议 — {{EXPERIMENT_NAME}}

> **本文档用途**：把一个实验从"想法"固化成"可交给远端 Agent / 云端机器照做"的协议。
> 云端 Agent 只读本文档 + `docs/research_background.md`，就应该能独立跑完并把产物放对位置。
> 本文档同时是**时间与成本预算表**——用来判断一轮实验值不值得跑。
> 最后更新：{{LAST_UPDATED}}　|　状态：草稿 / 已定稿 / 进行中 / 已完成

<!--
填写说明（成稿时删除）：
- 三个必须量化的东西：**配置空间有多大**（多少格）、**每格多久**、**总共多久**。
  缺任何一个，云端排队策略与"要不要砍格子"的决策都无从谈起。
- 参数表要写**默认值**与**本次取值**两列。只写本次取值，三个月后无法判断哪些是刻意设置、
  哪些是继承的默认。
- 判定标准必须**在跑之前**写死。跑完再定"什么算支持假说"，会不自觉地把结果读成想要的样子。
-->

## 1. 目标与待检验假说

| 假说 | 预测（若成立应观察到） | 判定标准（跑之前定死） |
|------|----------------------|----------------------|
| H1: [TODO] | [TODO] | [TODO: 指标 + 阈值 + 统计口径（如 pooled p<0.05）] |
| H2: [TODO] | [TODO] | [TODO] |

**本实验不能回答的问题**：[TODO: 明确边界，避免结果被过度解读，也避免云端 Agent 顺手扩实验]

---

## 2. 配置空间（跑哪些格）

**变量与取值**：

| 变量 | 取值 | 说明 |
|------|------|------|
| [TODO: 变量 1] | [TODO: 如 0.1 / 0.2 / 0.4] | [TODO] |
| [TODO: 变量 2] | [TODO] | [TODO] |

**格子总数**：`[TODO: 如 3×3×3 = 27 格]`

**已跑过的格子**（复用，不重跑）：

| 格子代码 | 来源 | 备注 |
|---------|------|------|
| [TODO: 如 格子代号 A / 格子代号 B] | [TODO: 哪个旧实验] | [TODO: 复用条件是否完全一致] |

**本轮待跑**：`[TODO: 27 - 6 = 21 格] × {{REPEATS}} 重复 = {{TOTAL_RUNS}} 次运行`

> 复用旧格子的前提是协议完全一致（同数据切分、同 epoch 数、同评测口径）。
> 只要有一项不同，就必须重跑——否则结果不可比，且事后无法追溯是哪一项不同。

---

## 3. 参数表

| 参数 | 默认值 | 本次取值 | 说明 |
|------|--------|---------|------|
| [TODO: 如 epochs] | [TODO] | [TODO] | [TODO] |
| [TODO: batch_size] | | | |
| [TODO: lr] | | | |
| [TODO: 数据切分方式] | | | [TODO: 切分方式对结论影响大时特别标注] |
| seed | — | [TODO: 如 [0,1,2]] | 每格每 seed 一个 run |

**命令模板**（云端 Agent 直接照抄，把 `[TODO: ...]` 处替换成实际值）：

```bash
python [TODO: run.py 子命令] \
    --config   [TODO: configs/xxx.yaml] \
    --[TODO: 变量1] {{V1}} \
    --[TODO: 变量2] {{V2}} \
    --seed     {{SEED}} \
    --out      results/{{EXPERIMENT_SLUG}}/{{GRID_CODE}}/{{RUN_ID}}   # 目录名 = run_id，见 §6
```

**固定项**（所有格子必须一致，不得因调试而临时改动）：[TODO: 列出]

---

## 4. 重复与统计口径

- **每格重复**：{{REPEATS}} 次，seed = [TODO]
- **汇总方式**：先算每格 mean ± std，再做跨格比较
- **显著性**：[TODO: 用什么检验、怎么合并（如 pooled test）、样本量多少]
- **异常 run 处理**：先记录，不静默丢弃。[TODO: 明确什么情况算失败 run、失败后是否重跑]

> 重复数不是形式主义：n=3 时一个 run 崩掉就会让该格的标准差失真。云端 Agent
> 遇到失败 run **必须**在 `run.json` 的 `note` 里注明，并报告给用户，不要自动补齐。

---

## 5. 时间估算

| 阶段 | 单次耗时 | 次数 | 小计 |
|------|---------|------|------|
| [TODO: 预训练] | [TODO: 如 25 min] | [TODO] | [TODO] |
| [TODO: 下游评测] | [TODO] | [TODO] | [TODO] |
| **合计** | — | — | **{{TOTAL_TIME}}** |

- **实测依据**：[TODO: 这个单次耗时是从哪次运行测出来的，别拍脑袋]
- **并行策略**：[TODO: 串行 / 按 GPU 数并行；并行时显存与 IO 是否成为瓶颈]
- **超时保护**：[TODO: 单 run 超过多少小时视为卡死]

---

## 6. 预期输出布局

每个 run 一个目录，**目录名 = `run_id`**（格式 `{YYYYMMDD}-{HHMM}-{kebab-name}`，见 `run.schema.json`；
`kebab-name` 里带上 seed 标识，如 `...-seed0`，这样 `ls` 一格目录就能读出跑了哪些重复）：

```
results/{{EXPERIMENT_SLUG}}/
├── {{GRID_CODE}}/                            # 一格一目录
│   ├── {{RUN_ID}}/                           # 目录名 = run_id（自带 seed 标识）
│   │   ├── run.json              # 溯源记录（必填，schema: run.schema.json）
│   │   ├── config.resolved.yaml  # 已解析配置快照（= run.json 的 config 字段）
│   │   ├── history.json          # 逐 epoch 训练曲线
│   │   ├── metrics.csv           # 评测指标（列名自由——刻意不做标准化，理由见 run.schema.json 顶部说明）
│   │   ├── train.log
│   │   └── ckpt/                 # 权重（通常不入库）
│   └── summary.csv               # 该格全部 run（各 seed）的 mean ± std
├── summary_all.csv               # 全格子汇总（画图脚本的唯一输入）
└── README.md                     # 本轮实验结论 + 异常记录
```

**产物去向**：`results/` 是 run 脚本与 plot 脚本之间的唯一桥梁——plot 脚本只读 `results/`，
不读 `ckpt/`、不读日志。汇总 CSV 的列名在本项目内自洽即可。

> **与 `tools/check_conventions.py` 的关系**：本布局里 `run.json` 位于
> `results/<实验名>/<格>/<run_id>/`，比扁平的 `results/<实验名>/run.json` **深两层**。
> 该脚本的 `run_json` 检查**两种布局都认**：先试扁平位置，没有再向下探一至两层，
> 并对探到的**每一份** `run.json` 校验必填字段。所以本布局可以放心用。
> **不要为了迁就工具而压平布局**——扁平化会丢掉「复用旧格子」与「每格跨 seed 汇总」这两个能力。

---

## 7. 完成判据（Definition of Done）

- [ ] 所有待跑格子 × {{REPEATS}} 重复完成，失败 run 已记录并报告
- [ ] 每个 run 目录都有合法的 `run.json`（必填字段齐全）
- [ ] `summary_all.csv` 生成，且行数 = 格子数
- [ ] `results/.../README.md` 写清：跑了什么、观察到什么、**哪些假说被支持/被证伪**
- [ ] 原始日志已归档（挂了 cloud 模块时，返回包按 `cloud/archive/` 的归档规则存放；未挂载则写明存放位置）
- [ ] 结论若改变了 `docs/research_background.md` §8/§9 的判断，同步更新该文档（含"最后更新"日期）

---

## 8. 风险与预案

| 风险 | 概率 | 预案 |
|------|------|------|
| [TODO: 如显存溢出] | 中 | [TODO: 降 batch / 减 seq_len 并记录] |
| [TODO: 云端实例中断] | 中 | [TODO: 断点续跑策略；已完成格子不重跑] |
| [TODO] | | |
