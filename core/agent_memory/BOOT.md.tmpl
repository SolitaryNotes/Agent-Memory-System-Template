# BOOT.md — {{PROJECT_NAME}} 扩展状态快照

> 本文件是 `CLAUDE.md`（内核）的扩展。当内核信息不足以完成任务时读取。
> 完整细节见 `current_state.md`；历史背景见 `session_logs/`。
> 启动顺序：`CLAUDE.md` → **本文件** → `current_state.md` → `git log --oneline -15` → `session_logs/` → `plans/`
> （与内核 `CLAUDE.md` 的 Boot Sequence 一节逐项一致；停止规则那一条也在内核里，本文件不重复。）

**Updated**: {{DATE}} ⚠️ *脚手架初始化：尚无真实会话，本日期由初始化脚本写入，**不代表项目有进展**；**首个真的产出了东西的会话**之后，才改为真实更新日期并删掉本段斜体* | **Stage**: {{CURRENT_STAGE}}{{BRANCH_ITEM}}

> ⚠️ 上句的限定词不是修辞。**只问了几句、没有产出的会话不算**——那种会话按内核规定本来就不写
> session log。此时若为了"把日期换成真实值"而删掉这段标注、顺手去填下面那张状态表，
> 就是**编造**：校验器只认「脚手架初始化」这个字面标注，删掉它，一份全是 TODO 的状态文件
> 会被判成「更新于 0 天前」——**一条文档指令把防编造守卫变成了假绿灯**（实测复现过）。
> **没有内容可填时，保留这段标注才是正确的。**

---

## 1. 当前状态

| 项 | 状态 |
|---|---|
| 优先级 | {{PRIORITY}} |
| 当前任务 | {{CURRENT_TASK}} |
| 阻塞点 | {{BLOCKER}} |
| 最近提交 | 见 `git log --oneline -15`（尚未 `git init` 时此行为空，属正常） |
| 活跃计划 | [TODO: `plans/<文件名>`，无则填「无」] |
| 最近会话 | [TODO: `session_logs/<文件名>`；尚无任何会话记录则填「无」] |
| 待办 | 见 `current_state.md` 的待办树 |

---

## 2. 任务 → 文件路由表 ⭐

> **本表是全文信息密度最高的一节。** 接到任务先在这里找对应行。
>
> - 标着 `[TODO: ...]` 的行 = **尚未填写的行**，不是"找不到"。按你的实际情况把它改写成真实内容。
> - 真的没有对应行时，按「同类就近」放置，**并在本表补一行**。
> - 若你为了完成任务一路读到了 `current_state.md` 甚至 `session_logs/`，说明本表缺行——顺手补上。
>   **唯一的例外（先问这一句）**：项目本身**还没有真实内容**吗（没有实验/数据/结论）？
>   - 是 → 本表的 `[TODO]` 行**一个都别改写**，那等于编造。停下来问用户
>     「这个项目要做什么」。也不要去找"缺失的行"——这里没有缺失，只有尚未开始。
>   - 否 → 正常维护：缺行就补，`[TODO]` 行按实际情况改写。
>
>   **上面两种情形都成立的一条**：标注了「没装则删掉本行」的**未安装模块**的行，
>   任何时候都可以直接删——它**不是** `[TODO]` 行，不受"空项目别动表"的约束。
>   留着一条指向不存在目录的路由，比删掉它更容易把读者引进死胡同。
>   （冷启动验收实测：这条没写清时，两个都合规的 Agent 会产出不同的仓库状态。）
>
>   内核 `CLAUDE.md` 的 Boot Sequence 一节对此有完整说明。

| 任务类型 | 需要改动的文件 |
|---|---|
| {{TASK_TYPE_1}} | {{FILES_1}} |
| {{TASK_TYPE_2}} | {{FILES_2}} |
| {{TASK_TYPE_3}} | {{FILES_3}} |
| 新增实验 | `experiments/run/<name>.py` + `configs/<name>.yaml` + `results/<name>/`（含 `run.json`；目录布局见 `docs/experiment_protocol.md`）（**仅当本项目启用了 research 模块**；没装则这些路径不存在，删掉本行即可） |
| 新增图表 | `experiments/plot/{exploratory,curated}/{NN}-{name}.py` + `figures/` 下同名目录（**仅当本项目启用了 research 模块**；没装则删掉本行即可） |
| 调整目录结构 | 本表 + 本文件 §4 目录结构 + `current_state.md` §8 结果与产物 |
| 写会话记录 | 复制 `agent_memory/session_logs/_TEMPLATE.md` → `session_YYYYMMDD_<slug>.md`，**不要直接改模板** |
| 写背景/协议文档 | `docs/`（研究背景、实验协议、文献路线） |
| 写组会/论文笔记 | `meetings/`（复制 `_TEMPLATE-*.md`，**不要直接改模板**）（**仅当本项目启用了 meetings 模块**；没装则 `meetings/` 不存在，删掉本行即可） |
| 同步记忆 | `agent_memory/README.md` §六 更新检查表 |
| 远程实验 | `cloud/README.md` → `cloud/docs/cloud_coordination.md`（**仅当本项目启用了 cloud 模块**；没装则这些路径不存在，删掉本行即可） |

---

## 3. 关键设计不变量

> 这些是本项目的**硬约束**。改动它们前必须先在 session log 里记录理由。

- {{INVARIANT_1}}
- {{INVARIANT_2}}
- {{INVARIANT_3}}

---

## 4. 目录结构

```
{{DIR_TREE}}
```

---

## 5. 已知问题与技术债

| 问题 | 影响 | 状态 |
|---|---|---|
| {{ISSUE_1}} | {{IMPACT_1}} | {{STATUS_1}} |

> 已解决的条目**不要删除**，改为 `~~删除线~~（YYYY-MM-DD 已解决）`——保留决策痕迹。

---

## 6. 风格约定

> 见内核 `CLAUDE.md` 的 **Style** 一节。**此处不重复。**
> 同一份内容写在两个文件里必然漂移——这正是本系统用 `tools/sync_agents_md.py`
> 消除掉的那类问题，风格约定没有理由例外。

---

## 7. 快速调试命令

```bash
{{DEBUG_CMDS}}
```

---

## 8. 已弃用模块（不要 import）

- （暂无）

> 弃用而非删除：保留在磁盘上以维持 git 历史。若确认永不再用，在此列出。

---

## 9. References

- 内核：`../CLAUDE.md`
- 完整手册：`current_state.md`
- 记忆系统约定：`README.md`
- 活跃计划：[TODO: `plans/<文件名>`，无则填「无」]
- Git：{{GIT_REMOTE}}（**没有远程仓库就填「无（仅本地）」**——不要为了填满这一项而编一个 URL）
