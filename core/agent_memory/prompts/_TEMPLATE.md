<!-- ⚠️ 这是模板，不是任务简报。复制成 YYYY-MM-DD-<slug>.md 后再填写。 -->
<!-- 简报一旦发出去就不再修改；要改就新写一份，让旧的保留下来作为决策痕迹。 -->

# Agent Prompt — [任务名]

> 用法：把本文件**整段**交给一个**新开的** Agent 会话。
> 它启动后会先执行 Boot Sequence 加载上下文，然后按下面的步骤执行。
> **背景**：[为什么有这个任务——通常是「上一个方案因为 X 失败了」]

---

## 启动顺序
按序读以下文件，不要跳读：
1. `CLAUDE.md`（自动加载）
2. `agent_memory/BOOT.md` — 尤其 §2 任务路由表
3. [本任务相关的具体文件]

## 已知前提

| 假设 | 验证方式 | 结论 |
|---|---|---|
| [假设 1] | [怎么验证的] | ✅ / ❌ [结论] |

> 把**已被证伪**的假设也写进来。这是防止新 Agent 重走死路的最有效手段。

## 任务

### Step 1: [动作]
[做什么]

### Step 2: [动作]
```bash
[确切的命令]
```

### Step 3: 评估
**对比基准**

| 指标 | 基准 | 本次 |
|---|---|---|
| [metric] | [值] | ? |

## 报告格式 ⭐

> **按这个骨架回报，不要自由发挥。** 把 `?` 换成实测值。

```markdown
## 执行结果
- 状态：✅ 完成 / ⚠️ 部分完成 / ❌ 失败
- [关键数字]

## 判据
| 检查项 | 结果 |
|---|---|
| [检查 1] | ✅/❌ |

## 异常与偏离
[如实写。没有就写「无」。**严禁编造数字。**]

## 产物
| 文件 | 说明 |
|---|---|
| [path] | [是什么] |
```

## 暂停条件

出现以下情况**立即停下并回报**，不要自行决定：
- [条件 1，如：基准指标与预期差距 > X%]
- [条件 2，如：需要修改本任务范围外的文件]
