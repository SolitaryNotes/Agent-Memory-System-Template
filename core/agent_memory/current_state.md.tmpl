# {{PROJECT_NAME}} — 完整手册

> **本文件是动态索引，不是一次性文档。**
> 后续 Agent 执行新任务时，若发现本文件未覆盖所需领域，**可直接在本文件夹下创建专题文档**
> （如 `gpu_setup.md`、`data_pipeline.md`），并在末尾「§10 专题文档索引」加一行链接。
> ⚠️ 前提：**项目已有真实内容**。空脚手架阶段不要为了「补一节」而编造专题——先问用户。
>
> 这是**记忆系统**唯一认可的扩展方式——只能往 `agent_memory/` 里加专题文档，
> **不要**在 `agent_memory/` 下另开一层子目录（如再建一个 notes 文件夹）。
>
> 本条**不限制项目本身**的目录结构——`experiments/`、`configs/`、`cloud/` 等
> 由项目实际需要决定，与记忆系统无关。

**最后更新**: {{DATE}}（{{UPDATE_REASON}}） ⚠️ *脚手架初始化：尚无真实会话，本日期由初始化脚本写入，**不代表本文件已被更新过**；**首个真的产出了东西的会话**之后，才改为真实日期并删掉本段斜体（只问了几句、没有产出的会话不算——理由见 `BOOT.md` 顶部那段）*
**当前阶段**: {{CURRENT_STAGE}}
**优先级**: {{PRIORITY}}
**活跃计划**: [TODO: `plans/<文件名>`；无活跃计划则填「无」]

**更新约定**：本文件只在**结构变化**时更新（新增模块、结论翻转、目录调整）。
日常进展写进 `session_logs/`，不要往这里堆流水账。

---

## 1. 目标与范围

### 1.1 总体目标
{{GOAL}}

### 1.2 子问题 / 子目标

| 编号 | 内容 | 当前状态 |
|---|---|---|
| Q1 | {{Q1}} | {{Q1_STATUS}} |
| Q2 | {{Q2}} | {{Q2_STATUS}} |

### 1.3 明确排除（不做什么）
- ❌ {{NOT_DOING_1}}

> 写下「不做什么」和写下「做什么」同样重要——它阻止后续 Agent 反复提议已经被否掉的方向。

---

## 2. 模块清单

> 每个模块一行：文件 / 状态 / 说明。**状态列**只填下面三者之一：✅ 完成、🚧 进行中、🗑️ 已弃用。
> 不要保留任何未填写的行——空行配一个状态符号等于声明了一件没发生过的事。

### 2.1 {{MODULE_1}}

| 文件 | 状态 | 说明 |
|---|---|---|
| [TODO: 文件路径] | [TODO: 状态（按上方图例填）] | [TODO: 说明] |

---

## 3. 核心变量 / 配置

| 变量 | 位置 | 可选值 | 说明 |
|---|---|---|---|
| {{VAR}} | {{WHERE}} | {{VALUES}} | {{DESC}} |

---

## 4. 关键设计原则

### 4.1 {{PRINCIPLE_1}}
{{PRINCIPLE_1_BODY}}

> 每条原则都要写**理由**，而不只是结论。理由消失时，原则才可以被推翻。

---

## 5. 快速开始

```bash
{{QUICK_START_CMDS}}
```

---

## 6. 已知问题与技术债

### 6.1 {{CATEGORY}}
- {{ISSUE}}

> 已解决项改为 `~~问题~~（YYYY-MM-DD 已解决）`，保留决策痕迹。

---

## 7. 待办树

> 用状态 emoji 分组，每条 `- [ ]` / `- [x]` + 加粗主题 + 冒号 + 细节 + 指向 session log。

### 🟢 进行中（{{DATE_RANGE}}）
- [ ] **{{TODO}}**：{{DETAIL}}。详见 [TODO: `session_logs/<日志文件名>`；本次尚无日志则填「尚无」]

### 🔵 计划中
- [ ] {{PLANNED}}

### ✅ 已完成
<!-- 完成项移到这里并把 `- [ ]` 改成 `- [x]`。没有就留空——
     不要放占位条目：一个「已完成但不知道完成了什么」的勾选项会逼后续 Agent 编造内容。 -->

---

## 8. 结果与产物

```
{{RESULTS_TREE}}
```

**核心发现**
- {{FINDING}}

---

## 9. 会话日志索引 ⭐

> **每次新增 session log 后必须在此补一行。** 最近的在最下方，用**加粗**标记当前阶段。
>
> ⚠️ **一份日志都没有时，本表只有表头、一行数据都没有——那就是正确状态，不是待填的空位。**
> 不要为了让表格"看起来完整"而留一行格式示例：示例行的形态与真实记录**完全一致**，
> 后续会话会把它读成「这里确实发生过一次会话」，并据此推断出一段不存在的历史。

行的格式（**只在真的写了日志时才用**，别提前写进上面的表里）：

    | YYYY-MM-DD | [`session_YYYYMMDD_<slug>.md`](session_logs/) | 一句话摘要 |

| 日期 | 文件 | 摘要 |
|---|---|---|

---

## 10. 专题文档索引 ⭐

> 新增专题文档后必须在此登记。

| 文档 | 主题 | 何时读 |
|---|---|---|
| {{TOPIC_DOC}} | {{TOPIC}} | {{WHEN}} |
